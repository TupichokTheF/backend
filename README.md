# Backend

Backend-приложение для интернет-магазина на Python с использованием FastAPI, PostgreSQL и Redis. EZ :frog:

## Описание

REST API сервер с полным функционалом для e-commerce платформы: каталог товаров, корзина, избранное, авторизация пользователей.

## Технологии

- **Python** — основной язык
- **FastAPI** — веб-фреймворк
- **PostgreSQL** — основная база данных
- **Redis** — кэширование и сессии
- **JWT** — Access/Refresh токены для аутентификации
- **RabbitMQ** — Брокер сообщений для микросервисов
  
## Возможности

- Аутентификация и авторизация (JWT)
- Регистрация и выход из системы
- Обновление Access токена по Refresh токену
- CRUD операции с продуктами
- Пагинация каталога
- Полнотекстовый поиск по триграммам (PostgreSQL)
- Корзина покупок
- Избранные товары

## Структура проекта
```
├── app/
│   ├── api/
│   │   ├── __init__.py
│   │   ├── api_router.py       # Главный роутер API
│   │   ├── routes/             # Эндпоинты
│   │   └── services/           # Сервисы бизнес-логики
│   │
│   ├── core/
│   │   ├── __init__.py
│   │   ├── authorization.py    # Логика авторизации
│   │   ├── security.py         # JWT, хэширование паролей
│   │   └── settings.py         # Конфигурация приложения
│   │
│   ├── crud/
│   │   ├── __init__.py
│   │   ├── products.py         # CRUD продуктов
│   │   ├── tokens.py           # Работа с токенами
│   │   └── users.py            # CRUD пользователей
│   │
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── cart.py             # Схемы корзины
│   │   ├── logger.py           # Схемы логирования
│   │   ├── products.py         # Схемы продуктов
│   │   ├── tokens.py           # Схемы токенов
│   │   └── user.py             # Схемы пользователей
│   │
│   ├── middleware/
│   │   ├── __init__.py
│   │   └── logging_middleware.py  # Middleware логирования
│   │
│   ├── __init__.py
│   ├── main.py                 # Точка входа
│   ├── database.py             # Подключение к БД
│   └── models.py               # SQLAlchemy модели
│
├── .env                        # Переменные окружения
└── README.md
```

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/TupichokTheF/backend.git
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте `.env` файл (подключение к PostgreSQL, Redis, секретные ключи)

5. Запустите приложение:
```bash
uvicorn app.main:app --reload
```

## API

После запуска документация доступна по адресам:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

## История изменений

## 01.02.2026-04.02.2026
- Добавлен микросервис обработки заказов
- Добавлен микросервис отправки email уведомлений
- Добавлена двухфакторная аунтефикация

### 29.01.2026 – 31.01.2026
- Работа с избранными продуктами через фронт
- Работа с корзиной через фронт
- Поиск по триграммам (PostgreSQL)

### 28.01.2026
- Интеграция Redis
- Функционал корзины
- Взаимодействие с фронтендом
- Пагинация продуктов

### 25.01.2026
- Регистрация и выход из системы
- Обновление Access токена по Refresh токену
- Разделение на сервисы и репозитории

### 24.01.2026
- Авторизация и аутентификация

### 23.01.2026
- Pydantic-схемы
- CRUD операции
- Роуты для продуктов
- Зависимости проекта

### 22.01.2026
- Инициализация проекта
- Структура и база данных
- Файл настроек



